# Results

This directory is reserved for analysis outputs and reports. Existing output files were not moved or overwritten because the request requires preserving every original file.

See `plots/README.md` for the locations of the existing generated figures.
